const test = 0;

const str = 'string';

const obj = {
  a: 1,
  b: 2,
  c: 3,
};

export const misc = (): any => {
  return {
    test,
    str,
    obj,
  };
};

export const sum = (a: number, b: number) => {
  return a + b;
};
